source "https://gems.ruby-china.com"   # 国内镜像，下载更快更稳定

gem "github-pages", group: :jekyll_plugins

# Minimal Mistakes 必须的插件
gem "jekyll-include-cache", group: :jekyll_plugins

# Windows 用户需要（Mac 可以保留，无影响）
gem "wdm", "~> 0.1.0" if Gem.win_platform?

# 时区数据（Mac 通常不需要，但保留更安全）
gem "tzinfo-data"